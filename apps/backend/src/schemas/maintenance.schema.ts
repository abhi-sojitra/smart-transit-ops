/** Single source of truth — Maintenance Module schema (`maintenance` collection). */
export {
  Maintenance,
  MaintenanceSchema,
  MaintenanceAttachmentEmbedded,
  type MaintenanceDocument,
} from '../modules/maintenance/schema/maintenance.schema';
